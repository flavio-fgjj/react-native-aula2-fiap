import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    marginTop: 20,
  },
  textInput: {
    backgroundColor: '#FFFFFF90',
    padding: 10,
    borderRadius: 8,
    minHeight: 40,
  },
  errorText: {
    marginTop: 5,
    color: '#FFF',
    fontStyle: 'italic',
  },
});

export {styles};
